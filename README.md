# RPM specs for VictoriaMetrics and VictoriaLogs on Fedora

There are a few RPM packages for VictoriaMetrics out there (including
the one spec inside the tree itself), but the quality is questionable.
Anyway, here we go.
